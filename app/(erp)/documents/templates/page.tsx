"use client";

import { FileText, Download, Eye, ShieldCheck } from "lucide-react";
import { generateBrandedPdf, samplePdfData, type PdfTemplate } from "@/lib/pdf/generator";
import { Button } from "@/components/ui";

const templates: Array<[PdfTemplate,string]> = [["estimation","Estimation sheet"],["quotation","Quotation"],["invoice","Invoice"],["receipt","Payment receipt"],["purchase_order","Purchase order"],["delivery_note","Delivery note"],["packing_list","Packing list"],["service_report","Service report"],["employee_letter","Employee letter"],["experience_certificate","Experience certificate"],["leave_approval","Leave approval"],["payment_voucher","Payment voucher"]];
export default function TemplatesPage() { const download = (template: PdfTemplate) => generateBrandedPdf({ ...samplePdfData, template, documentNumber: `${template.slice(0,3).toUpperCase()}-2026-00001` }); return <div className="mx-auto max-w-[1400px] p-4 md:p-7"><div className="mb-6"><div className="text-[10px] font-bold uppercase tracking-widest text-teal-600">Document Center</div><h1 className="mt-1 text-2xl font-bold">Branded PDF templates</h1><p className="mt-1 text-xs text-[var(--muted)]">Controlled, print-ready business documents using MedTech’s standard identity.</p></div><div className="mb-5 flex items-center gap-3 rounded-2xl border border-teal-200 bg-teal-50 p-4 text-sm text-teal-800 dark:border-teal-900 dark:bg-teal-950/40 dark:text-teal-200"><ShieldCheck className="h-5 w-5" /><div><b>Template controls enabled.</b><span className="ml-1 text-xs">Numbers, approvers and record data are generated from the source transaction.</span></div></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{templates.map(([key,label]) => <div key={key} className="rounded-2xl border bg-[var(--panel)] p-5 shadow-soft"><div className="mb-6 flex items-start justify-between"><div className="rounded-xl bg-teal-50 p-3 text-teal-600 dark:bg-teal-950/50"><FileText className="h-6 w-6" /></div><span className="rounded-full bg-emerald-50 px-2.5 py-1 text-[10px] font-semibold text-emerald-700 dark:bg-emerald-950 dark:text-emerald-300">Active</span></div><h2 className="font-bold">{label}</h2><p className="mt-1 text-xs text-[var(--muted)]">A4 · MedTech corporate · Version 1.0</p><div className="mt-5 flex gap-2"><Button className="flex-1" onClick={() => download(key)}><Download className="h-4 w-4" /> Generate sample</Button><Button variant="secondary" title="Preview"><Eye className="h-4 w-4" /></Button></div></div>)}</div></div>; }
