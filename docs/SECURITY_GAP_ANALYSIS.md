# Security Gap Analysis

Scope: local MedTech ERP demo project only. No external systems were tested, contacted, scanned, uploaded to, or attacked.

| Finding ID | Area | Risk | Severity | Evidence / file path | Recommended fix | Status | Fixed in commit/session or pending |
|---|---|---|---|---|---|---|---|
| SEC-001 | Redirects / navigation | Login and auth callback accepted any path beginning with `/`; protocol-relative paths such as `//host` can become unsafe navigation in browser contexts. | High | `app/login/page.tsx`, `app/auth/callback/route.ts` | Centralize same-origin relative path validation and reject protocol-relative or backslash paths. | Fixed | Fixed this session in `lib/security.ts`, `app/login/page.tsx`, `app/auth/callback/route.ts` |
| SEC-002 | CSP / security headers | Baseline headers existed, but CSP, DNS prefetch control, and cross-domain policy headers were missing. | High | `next.config.ts` | Add local-demo-compatible CSP and hardening headers. | Fixed | Fixed this session in `next.config.ts` |
| SEC-003 | Excel imports | Spreadsheet imports had no file size or row-count limits, allowing browser memory exhaustion in local demo mode. | High | `lib/export/excel.ts`, `components/sales-costing-workspace.tsx`, `components/migration-readiness-workspace.tsx` | Enforce small local import limits and return controlled errors. | Fixed | Fixed this session |
| SEC-004 | Backup / restore | Restore validated broad shape only and could clear existing local data before discovering an oversized item. | High | `lib/demo-store.ts`, `components/local-data-tools-workspace.tsx` | Validate item count and value sizes before clearing localStorage. | Fixed | Fixed this session in `lib/demo-store.ts` |
| SEC-005 | Demo session handling | Demo middleware trusts the presence of `medtech_demo_session`; localStorage remains the source of role/session details. This is acceptable for local demo, not production. | High | `lib/supabase/middleware.ts`, `lib/demo-auth.ts` | Production must use server-authenticated sessions, HttpOnly cookies, MFA, and server-side authorization. | Pending | Pending production hardening |
| SEC-006 | Demo passwords | Local demo users store temporary passwords in localStorage and fixtures. This is explicitly local-demo only. | High | `lib/demo-auth.ts`, `tests/fixtures/test-users.json`, `docs/TESTING.md` | For production, remove client-side password storage and use an identity provider/Auth service with password policies and MFA. | Pending | Pending production hardening |
| SEC-007 | Supabase dependency presence | Supabase clients and scripts exist for production/staging paths, although current requested mode is local-only with blank env values. | Medium | `lib/supabase/*`, `scripts/create-test-users.mjs`, `.env.production` | Keep env blank for local demo; gate production scripts and document server-only secrets. | Documented | Pending production environment controls |
| SEC-008 | RBAC enforcement depth | UI actions use local role checks, but client-side RBAC is advisory in local demo mode. | High | `lib/erp-security.ts`, `components/module-workspace.tsx`, `components/approval-matrix-workspace.tsx` | Production must enforce RBAC server-side on every API/data mutation. | Pending | Pending production hardening |
| SEC-009 | API routes | `/api/pdf/sample` is protected by the local demo session gate and returns controlled JSON `401` responses when unauthenticated; template input is allowlisted. | Medium | `app/api/pdf/sample/route.ts`, `lib/supabase/middleware.ts` | Keep allowlist; require server-side authorization in production if PDFs include real data. | Fixed for local demo | Pending production auth |
| SEC-010 | PDF generation | PDF data is generated from trusted app records and template enums; generated PDFs can contain demo PII-like text. | Medium | `lib/pdf/generator.ts`, `components/*workspace.tsx` | Keep local-only; production should classify, authorize, and watermark sensitive PDF exports. | Documented | Pending production controls |
| SEC-011 | Document downloads | Local download helpers create object URLs and revoke them; downloads are client-local. | Low | `lib/client-download.ts`, `app/(erp)/documents/templates/page.tsx` | Continue revocation; production should add access checks and immutable audit IDs. | Accepted | No immediate fix needed |
| SEC-012 | Attachment placeholders | Attachment/document metadata is demo data, not uploaded binary storage. | Medium | `lib/attachment-workflow.ts`, `lib/medtech-scope-data.ts` | Production upload path needs MIME sniffing, AV scanning, object storage ACLs, and size limits. | Pending | Pending production feature |
| SEC-013 | Error handling | Most local workflows catch parse/storage errors; some dynamic imports and client actions still rely on generic UI toasts. | Medium | `components/*workspace.tsx` | Add centralized error boundary and sanitized error reporting before production. | Pending | Pending production hardening |
| SEC-014 | Audit logging | Audit log is localStorage-backed and user-controlled in demo mode. | Medium | `lib/audit-store.ts` | Production must write append-only server-side audit records with actor/session/IP metadata. | Pending | Pending production hardening |
| SEC-015 | Docker runtime | Container runs as non-root and no-new-privileges is enabled; image still uses broad `node:22-alpine` base and no read-only filesystem. | Medium | `Dockerfile`, `docker-compose.yml` | Pin image digests, add read-only rootfs/tmpfs where practical, and scan images in CI. | Pending | Pending DevOps hardening |
| SEC-016 | Nginx | TLS, HSTS, no-sniff, frame deny, and API rate limit exist; CSP is enforced by Next app, not Nginx. | Low | `deploy/nginx.conf` | Mirror CSP at edge in production if Nginx terminates TLS. | Documented | Pending production deployment |
| SEC-017 | Dependency security | Local dependency inventory was inspected from `package.json` and `package-lock.json`; no internet-backed audit was run to avoid external calls. | Medium | `package.json`, `package-lock.json` | Run `npm audit` or SCA in an isolated approved pipeline. Do not run `npm audit fix --force` blindly. | Pending | Pending approved dependency scan |
| SEC-018 | Secrets | No real secrets were found in source scan. `.env.production` contains local-demo placeholder values and must not be used as production secrets. | High | `.env.example`, `.env.production`, `scripts/create-test-users.mjs` | Generate real production secrets in a secret manager; keep service role keys server-only. | Documented | Pending production secret management |
| SEC-019 | Forms | RecordModal enforces required primary fields, email, password minimums, and per-workspace validation exists for critical flows. | Medium | `components/record-modal.tsx`, `lib/validation.ts`, tests | Add server-side validation when APIs are introduced. | Partially fixed | Pending production APIs |
| SEC-020 | Middleware auth boundaries | Demo mode protects pages with a cookie gate and permits `/api/health`; production mode uses Supabase session lookup. | Medium | `middleware.ts`, `lib/supabase/middleware.ts` | Keep local behavior; production should add route-level authorization, CSRF strategy, and security review. | Documented | Pending production hardening |

## Fixed This Session

- Added `safeLocalPath` redirect sanitizer.
- Replaced direct `startsWith("/")` redirect checks.
- Tightened demo cookie SameSite from `Lax` to `Strict`.
- Added local-demo CSP and additional security headers.
- Added Excel import size and row limits.
- Added controlled import error handling in costing and migration readiness imports.
- Added backup restore item count and value-size validation before clearing local demo data.
- Added unit tests for redirect, import, and backup hardening.
